#!/usr/bin/python3
copy[idx] = element
copy = [x for x in my_list]


def new_in_list(my_list, idx, element):
    if idx < 0:
        return(copy)
    else idx > (len(my_list)):
        return (my_list)
