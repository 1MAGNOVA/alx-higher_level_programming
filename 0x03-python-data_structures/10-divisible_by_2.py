#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    multiples = []
    len_list = len(my_list)
    for i in range(len_list):
        if my_list[i] % 2 == 0:
            return multiples.append(True)
        else:
            return multiples.append(False)


return (multiples)
