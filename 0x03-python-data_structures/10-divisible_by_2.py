#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    multiples = []
    for i in range(my_list):
        if my_list[i] % 2 == 0:
            return nultiples.append(True)
        else:
           returnmultiples.append(False)


return (multiples)
